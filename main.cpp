#include <SFML/Graphics.hpp>
using namespace sf;

int main(){
    //creation
    RenderWindow window(VideoMode(800,600), "SFML-Game")

    bool game = true;

    while (game == true)
    {
        
    }
    
    //clean your sh up
    window.close();
    return 0;
}